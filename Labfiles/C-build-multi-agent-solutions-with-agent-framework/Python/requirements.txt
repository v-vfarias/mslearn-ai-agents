python-dotenv
azure-identity==1.25.3
# Installs only core + foundry + openai (this lab doesn't need the other integrations
# the full agent-framework meta-package bundles). Pinned to a version tested together
# with a compatible agent-framework-core/agent-framework-openai resolution.
agent-framework-foundry==1.10.3
httpx
uvicorn
starlette
sse-starlette
fastapi
azure-ai-projects
azure-ai-agents
a2a-sdk==0.3.26
# openai 3.x replaced httpx with httpx2, which breaks azure-ai-projects'
# `import httpx`. Pinned until azure-ai-projects supports openai 3.
openai<3
